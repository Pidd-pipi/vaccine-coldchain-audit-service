const rows=document.querySelector('#rows'),message=document.querySelector('#message');async function load(){const r=await fetch('/api/v1/batches');const d=await r.json();rows.innerHTML=d.items.map(x=>`<tr><td>${x.id}</td><td>${x.vaccine}</td><td>${x.clinic}</td><td>${x.status}</td><td>${x.temperature_c.toFixed(1)}</td><td><button data-id="${x.id}">放行批次</button></td></tr>`).join('');rows.querySelectorAll('button').forEach(b=>b.onclick=()=>update(b.dataset.id))}async function update(id){const r=await fetch(`/api/v1/batches/${id}/status`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'released'})});message.textContent=r.ok?'批次状态已更新':'更新失败';if(r.ok)load()}document.querySelector('#refresh').onclick=load;load().catch(()=>message.textContent='无法读取批次记录');
